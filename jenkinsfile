pipeline {

    agent {
     label 'Agent1'
     }

    tools {
      maven 'Maven'
     }   

    stages{
        stage ("Build")
        {
            steps {
                sh 'mvn clean package'
            }
        }
        post {
  success {
    // One or more steps need to be included within each condition's block.
  }
}

    }

}